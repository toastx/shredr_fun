//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const COMMIT_STEALTH_DISCRIMINATOR: u8 = 2;

/// Accounts.
#[derive(Debug)]
pub struct CommitStealth {
            /// Relayer paying for the transaction

    
              
          pub relayer: solana_address::Address,
                /// Stealth PDA to commit

    
              
          pub stealth_account: solana_address::Address,
                /// MagicBlock program

    
              
          pub magic_program: solana_address::Address,
                /// MagicBlock context account

    
              
          pub magic_context: solana_address::Address,
      }

impl CommitStealth {
  pub fn instruction(&self) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(&[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(4+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.relayer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.stealth_account,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.magic_program,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.magic_context,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let data = CommitStealthInstructionData::new().try_to_vec().unwrap();
    
    solana_instruction::Instruction {
      program_id: crate::SHREDR_PROGRAM_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
 pub struct CommitStealthInstructionData {
            discriminator: u8,
      }

impl CommitStealthInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: 2,
                  }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for CommitStealthInstructionData {
  fn default() -> Self {
    Self::new()
  }
}



/// Instruction builder for `CommitStealth`.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` relayer
                ///   1. `[writable]` stealth_account
          ///   2. `[]` magic_program
                ///   3. `[writable]` magic_context
#[derive(Clone, Debug, Default)]
pub struct CommitStealthBuilder {
            relayer: Option<solana_address::Address>,
                stealth_account: Option<solana_address::Address>,
                magic_program: Option<solana_address::Address>,
                magic_context: Option<solana_address::Address>,
                __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CommitStealthBuilder {
  pub fn new() -> Self {
    Self::default()
  }
            /// Relayer paying for the transaction
#[inline(always)]
    pub fn relayer(&mut self, relayer: solana_address::Address) -> &mut Self {
                        self.relayer = Some(relayer);
                    self
    }
            /// Stealth PDA to commit
#[inline(always)]
    pub fn stealth_account(&mut self, stealth_account: solana_address::Address) -> &mut Self {
                        self.stealth_account = Some(stealth_account);
                    self
    }
            /// MagicBlock program
#[inline(always)]
    pub fn magic_program(&mut self, magic_program: solana_address::Address) -> &mut Self {
                        self.magic_program = Some(magic_program);
                    self
    }
            /// MagicBlock context account
#[inline(always)]
    pub fn magic_context(&mut self, magic_context: solana_address::Address) -> &mut Self {
                        self.magic_context = Some(magic_context);
                    self
    }
            /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = CommitStealth {
                              relayer: self.relayer.expect("relayer is not set"),
                                        stealth_account: self.stealth_account.expect("stealth_account is not set"),
                                        magic_program: self.magic_program.expect("magic_program is not set"),
                                        magic_context: self.magic_context.expect("magic_context is not set"),
                      };
    
    accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
  }
}

  /// `commit_stealth` CPI accounts.
  pub struct CommitStealthCpiAccounts<'a, 'b> {
                  /// Relayer paying for the transaction

      
                    
              pub relayer: &'b solana_account_info::AccountInfo<'a>,
                        /// Stealth PDA to commit

      
                    
              pub stealth_account: &'b solana_account_info::AccountInfo<'a>,
                        /// MagicBlock program

      
                    
              pub magic_program: &'b solana_account_info::AccountInfo<'a>,
                        /// MagicBlock context account

      
                    
              pub magic_context: &'b solana_account_info::AccountInfo<'a>,
            }

/// `commit_stealth` CPI instruction.
pub struct CommitStealthCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// Relayer paying for the transaction

    
              
          pub relayer: &'b solana_account_info::AccountInfo<'a>,
                /// Stealth PDA to commit

    
              
          pub stealth_account: &'b solana_account_info::AccountInfo<'a>,
                /// MagicBlock program

    
              
          pub magic_program: &'b solana_account_info::AccountInfo<'a>,
                /// MagicBlock context account

    
              
          pub magic_context: &'b solana_account_info::AccountInfo<'a>,
        }

impl<'a, 'b> CommitStealthCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: CommitStealthCpiAccounts<'a, 'b>,
          ) -> Self {
    Self {
      __program: program,
              relayer: accounts.relayer,
              stealth_account: accounts.stealth_account,
              magic_program: accounts.magic_program,
              magic_context: accounts.magic_context,
                }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(4+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.relayer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.stealth_account.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.magic_program.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.magic_context.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let data = CommitStealthInstructionData::new().try_to_vec().unwrap();
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::SHREDR_PROGRAM_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.relayer.clone());
                        account_infos.push(self.stealth_account.clone());
                        account_infos.push(self.magic_program.clone());
                        account_infos.push(self.magic_context.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `CommitStealth` via CPI.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` relayer
                ///   1. `[writable]` stealth_account
          ///   2. `[]` magic_program
                ///   3. `[writable]` magic_context
#[derive(Clone, Debug)]
pub struct CommitStealthCpiBuilder<'a, 'b> {
  instruction: Box<CommitStealthCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CommitStealthCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(CommitStealthCpiBuilderInstruction {
      __program: program,
              relayer: None,
              stealth_account: None,
              magic_program: None,
              magic_context: None,
                                __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// Relayer paying for the transaction
#[inline(always)]
    pub fn relayer(&mut self, relayer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.relayer = Some(relayer);
                    self
    }
      /// Stealth PDA to commit
#[inline(always)]
    pub fn stealth_account(&mut self, stealth_account: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.stealth_account = Some(stealth_account);
                    self
    }
      /// MagicBlock program
#[inline(always)]
    pub fn magic_program(&mut self, magic_program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.magic_program = Some(magic_program);
                    self
    }
      /// MagicBlock context account
#[inline(always)]
    pub fn magic_context(&mut self, magic_context: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.magic_context = Some(magic_context);
                    self
    }
            /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = CommitStealthCpi {
        __program: self.instruction.__program,
                  
          relayer: self.instruction.relayer.expect("relayer is not set"),
                  
          stealth_account: self.instruction.stealth_account.expect("stealth_account is not set"),
                  
          magic_program: self.instruction.magic_program.expect("magic_program is not set"),
                  
          magic_context: self.instruction.magic_context.expect("magic_context is not set"),
                    };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct CommitStealthCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            relayer: Option<&'b solana_account_info::AccountInfo<'a>>,
                stealth_account: Option<&'b solana_account_info::AccountInfo<'a>>,
                magic_program: Option<&'b solana_account_info::AccountInfo<'a>>,
                magic_context: Option<&'b solana_account_info::AccountInfo<'a>>,
                /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

