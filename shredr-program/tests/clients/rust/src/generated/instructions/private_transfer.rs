//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const PRIVATE_TRANSFER_DISCRIMINATOR: u8 = 1;

/// Accounts.
#[derive(Debug)]
pub struct PrivateTransfer {
            /// Source stealth PDA, must sign

    
              
          pub source_pda: solana_address::Address,
                /// Destination stealth PDA

    
              
          pub destination_pda: solana_address::Address,
      }

impl PrivateTransfer {
  pub fn instruction(&self, args: PrivateTransferInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: PrivateTransferInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(2+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.source_pda,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.destination_pda,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = PrivateTransferInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::SHREDR_PROGRAM_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
 pub struct PrivateTransferInstructionData {
            discriminator: u8,
            }

impl PrivateTransferInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: 1,
                                }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for PrivateTransferInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
 pub struct PrivateTransferInstructionArgs {
                  pub amount: u64,
      }

impl PrivateTransferInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `PrivateTransfer`.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` source_pda
                ///   1. `[writable]` destination_pda
#[derive(Clone, Debug, Default)]
pub struct PrivateTransferBuilder {
            source_pda: Option<solana_address::Address>,
                destination_pda: Option<solana_address::Address>,
                        amount: Option<u64>,
        __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl PrivateTransferBuilder {
  pub fn new() -> Self {
    Self::default()
  }
            /// Source stealth PDA, must sign
#[inline(always)]
    pub fn source_pda(&mut self, source_pda: solana_address::Address) -> &mut Self {
                        self.source_pda = Some(source_pda);
                    self
    }
            /// Destination stealth PDA
#[inline(always)]
    pub fn destination_pda(&mut self, destination_pda: solana_address::Address) -> &mut Self {
                        self.destination_pda = Some(destination_pda);
                    self
    }
                    #[inline(always)]
      pub fn amount(&mut self, amount: u64) -> &mut Self {
        self.amount = Some(amount);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = PrivateTransfer {
                              source_pda: self.source_pda.expect("source_pda is not set"),
                                        destination_pda: self.destination_pda.expect("destination_pda is not set"),
                      };
          let args = PrivateTransferInstructionArgs {
                                                              amount: self.amount.clone().expect("amount is not set"),
                                    };
    
    accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
  }
}

  /// `private_transfer` CPI accounts.
  pub struct PrivateTransferCpiAccounts<'a, 'b> {
                  /// Source stealth PDA, must sign

      
                    
              pub source_pda: &'b solana_account_info::AccountInfo<'a>,
                        /// Destination stealth PDA

      
                    
              pub destination_pda: &'b solana_account_info::AccountInfo<'a>,
            }

/// `private_transfer` CPI instruction.
pub struct PrivateTransferCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// Source stealth PDA, must sign

    
              
          pub source_pda: &'b solana_account_info::AccountInfo<'a>,
                /// Destination stealth PDA

    
              
          pub destination_pda: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: PrivateTransferInstructionArgs,
  }

impl<'a, 'b> PrivateTransferCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: PrivateTransferCpiAccounts<'a, 'b>,
              args: PrivateTransferInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              source_pda: accounts.source_pda,
              destination_pda: accounts.destination_pda,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(2+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.source_pda.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.destination_pda.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = PrivateTransferInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::SHREDR_PROGRAM_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.source_pda.clone());
                        account_infos.push(self.destination_pda.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `PrivateTransfer` via CPI.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` source_pda
                ///   1. `[writable]` destination_pda
#[derive(Clone, Debug)]
pub struct PrivateTransferCpiBuilder<'a, 'b> {
  instruction: Box<PrivateTransferCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> PrivateTransferCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(PrivateTransferCpiBuilderInstruction {
      __program: program,
              source_pda: None,
              destination_pda: None,
                                            amount: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// Source stealth PDA, must sign
#[inline(always)]
    pub fn source_pda(&mut self, source_pda: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.source_pda = Some(source_pda);
                    self
    }
      /// Destination stealth PDA
#[inline(always)]
    pub fn destination_pda(&mut self, destination_pda: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.destination_pda = Some(destination_pda);
                    self
    }
                    #[inline(always)]
      pub fn amount(&mut self, amount: u64) -> &mut Self {
        self.instruction.amount = Some(amount);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = PrivateTransferInstructionArgs {
                                                              amount: self.instruction.amount.clone().expect("amount is not set"),
                                    };
        let instruction = PrivateTransferCpi {
        __program: self.instruction.__program,
                  
          source_pda: self.instruction.source_pda.expect("source_pda is not set"),
                  
          destination_pda: self.instruction.destination_pda.expect("destination_pda is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct PrivateTransferCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            source_pda: Option<&'b solana_account_info::AccountInfo<'a>>,
                destination_pda: Option<&'b solana_account_info::AccountInfo<'a>>,
                        amount: Option<u64>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

