/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SHREDR_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const COMMIT_AND_UNDELEGATE_STEALTH_DISCRIMINATOR = 3;

export function getCommitAndUndelegateStealthDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(COMMIT_AND_UNDELEGATE_STEALTH_DISCRIMINATOR);
}

export type CommitAndUndelegateStealthInstruction<
  TProgram extends string = typeof SHREDR_PROGRAM_PROGRAM_ADDRESS,
  TAccountRelayer extends string | AccountMeta<string> = string,
  TAccountStealthAccount extends string | AccountMeta<string> = string,
  TAccountMagicProgram extends string | AccountMeta<string> = string,
  TAccountMagicContext extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRelayer extends string
        ? WritableSignerAccount<TAccountRelayer> &
            AccountSignerMeta<TAccountRelayer>
        : TAccountRelayer,
      TAccountStealthAccount extends string
        ? WritableAccount<TAccountStealthAccount>
        : TAccountStealthAccount,
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      TAccountMagicContext extends string
        ? WritableAccount<TAccountMagicContext>
        : TAccountMagicContext,
      ...TRemainingAccounts,
    ]
  >;

export type CommitAndUndelegateStealthInstructionData = {
  discriminator: number;
};

export type CommitAndUndelegateStealthInstructionDataArgs = {};

export function getCommitAndUndelegateStealthInstructionDataEncoder(): FixedSizeEncoder<CommitAndUndelegateStealthInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({
      ...value,
      discriminator: COMMIT_AND_UNDELEGATE_STEALTH_DISCRIMINATOR,
    }),
  );
}

export function getCommitAndUndelegateStealthInstructionDataDecoder(): FixedSizeDecoder<CommitAndUndelegateStealthInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getCommitAndUndelegateStealthInstructionDataCodec(): FixedSizeCodec<
  CommitAndUndelegateStealthInstructionDataArgs,
  CommitAndUndelegateStealthInstructionData
> {
  return combineCodec(
    getCommitAndUndelegateStealthInstructionDataEncoder(),
    getCommitAndUndelegateStealthInstructionDataDecoder(),
  );
}

export type CommitAndUndelegateStealthInput<
  TAccountRelayer extends string = string,
  TAccountStealthAccount extends string = string,
  TAccountMagicProgram extends string = string,
  TAccountMagicContext extends string = string,
> = {
  /** Relayer paying for the transaction */
  relayer: TransactionSigner<TAccountRelayer>;
  /** Stealth PDA to commit and undelegate */
  stealthAccount: Address<TAccountStealthAccount>;
  /** MagicBlock program */
  magicProgram: Address<TAccountMagicProgram>;
  /** MagicBlock context account */
  magicContext: Address<TAccountMagicContext>;
};

export function getCommitAndUndelegateStealthInstruction<
  TAccountRelayer extends string,
  TAccountStealthAccount extends string,
  TAccountMagicProgram extends string,
  TAccountMagicContext extends string,
  TProgramAddress extends Address = typeof SHREDR_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CommitAndUndelegateStealthInput<
    TAccountRelayer,
    TAccountStealthAccount,
    TAccountMagicProgram,
    TAccountMagicContext
  >,
  config?: { programAddress?: TProgramAddress },
): CommitAndUndelegateStealthInstruction<
  TProgramAddress,
  TAccountRelayer,
  TAccountStealthAccount,
  TAccountMagicProgram,
  TAccountMagicContext
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SHREDR_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    relayer: { value: input.relayer ?? null, isWritable: true },
    stealthAccount: { value: input.stealthAccount ?? null, isWritable: true },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("relayer", accounts.relayer),
      getAccountMeta("stealthAccount", accounts.stealthAccount),
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("magicContext", accounts.magicContext),
    ],
    data: getCommitAndUndelegateStealthInstructionDataEncoder().encode({}),
    programAddress,
  } as CommitAndUndelegateStealthInstruction<
    TProgramAddress,
    TAccountRelayer,
    TAccountStealthAccount,
    TAccountMagicProgram,
    TAccountMagicContext
  >);
}

export type ParsedCommitAndUndelegateStealthInstruction<
  TProgram extends string = typeof SHREDR_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Relayer paying for the transaction */
    relayer: TAccountMetas[0];
    /** Stealth PDA to commit and undelegate */
    stealthAccount: TAccountMetas[1];
    /** MagicBlock program */
    magicProgram: TAccountMetas[2];
    /** MagicBlock context account */
    magicContext: TAccountMetas[3];
  };
  data: CommitAndUndelegateStealthInstructionData;
};

export function parseCommitAndUndelegateStealthInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCommitAndUndelegateStealthInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      relayer: getNextAccount(),
      stealthAccount: getNextAccount(),
      magicProgram: getNextAccount(),
      magicContext: getNextAccount(),
    },
    data: getCommitAndUndelegateStealthInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
