/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SHREDR_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const UNDELEGATION_CALLBACK_DISCRIMINATOR = 5;

export function getUndelegationCallbackDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(UNDELEGATION_CALLBACK_DISCRIMINATOR);
}

export type UndelegationCallbackInstruction<
  TProgram extends string = typeof SHREDR_PROGRAM_PROGRAM_ADDRESS,
  TAccountStealthAccount extends string | AccountMeta<string> = string,
  TAccountBufferAccount extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStealthAccount extends string
        ? WritableAccount<TAccountStealthAccount>
        : TAccountStealthAccount,
      TAccountBufferAccount extends string
        ? WritableAccount<TAccountBufferAccount>
        : TAccountBufferAccount,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UndelegationCallbackInstructionData = { discriminator: number };

export type UndelegationCallbackInstructionDataArgs = {};

export function getUndelegationCallbackInstructionDataEncoder(): FixedSizeEncoder<UndelegationCallbackInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({
      ...value,
      discriminator: UNDELEGATION_CALLBACK_DISCRIMINATOR,
    }),
  );
}

export function getUndelegationCallbackInstructionDataDecoder(): FixedSizeDecoder<UndelegationCallbackInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getUndelegationCallbackInstructionDataCodec(): FixedSizeCodec<
  UndelegationCallbackInstructionDataArgs,
  UndelegationCallbackInstructionData
> {
  return combineCodec(
    getUndelegationCallbackInstructionDataEncoder(),
    getUndelegationCallbackInstructionDataDecoder(),
  );
}

export type UndelegationCallbackInput<
  TAccountStealthAccount extends string = string,
  TAccountBufferAccount extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Stealth account being undelegated */
  stealthAccount: Address<TAccountStealthAccount>;
  /** MagicBlock buffer account */
  bufferAccount: Address<TAccountBufferAccount>;
  /** Payer for the callback */
  payer: TransactionSigner<TAccountPayer>;
  /** System Program */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getUndelegationCallbackInstruction<
  TAccountStealthAccount extends string,
  TAccountBufferAccount extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SHREDR_PROGRAM_PROGRAM_ADDRESS,
>(
  input: UndelegationCallbackInput<
    TAccountStealthAccount,
    TAccountBufferAccount,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): UndelegationCallbackInstruction<
  TProgramAddress,
  TAccountStealthAccount,
  TAccountBufferAccount,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SHREDR_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    stealthAccount: { value: input.stealthAccount ?? null, isWritable: true },
    bufferAccount: { value: input.bufferAccount ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("stealthAccount", accounts.stealthAccount),
      getAccountMeta("bufferAccount", accounts.bufferAccount),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getUndelegationCallbackInstructionDataEncoder().encode({}),
    programAddress,
  } as UndelegationCallbackInstruction<
    TProgramAddress,
    TAccountStealthAccount,
    TAccountBufferAccount,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedUndelegationCallbackInstruction<
  TProgram extends string = typeof SHREDR_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Stealth account being undelegated */
    stealthAccount: TAccountMetas[0];
    /** MagicBlock buffer account */
    bufferAccount: TAccountMetas[1];
    /** Payer for the callback */
    payer: TAccountMetas[2];
    /** System Program */
    systemProgram: TAccountMetas[3];
  };
  data: UndelegationCallbackInstructionData;
};

export function parseUndelegationCallbackInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUndelegationCallbackInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      stealthAccount: getNextAccount(),
      bufferAccount: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUndelegationCallbackInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
