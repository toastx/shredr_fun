/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { SHREDR_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_STEALTH_ACCOUNT_DISCRIMINATOR = 5;

export function getCloseStealthAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(CLOSE_STEALTH_ACCOUNT_DISCRIMINATOR);
}

export type CloseStealthAccountInstruction<
  TProgram extends string = typeof SHREDR_PROGRAM_PROGRAM_ADDRESS,
  TAccountBurner extends string | AccountMeta<string> = string,
  TAccountStealthAccount extends string | AccountMeta<string> = string,
  TAccountRentPayee extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBurner extends string
        ? ReadonlySignerAccount<TAccountBurner> &
            AccountSignerMeta<TAccountBurner>
        : TAccountBurner,
      TAccountStealthAccount extends string
        ? WritableAccount<TAccountStealthAccount>
        : TAccountStealthAccount,
      TAccountRentPayee extends string
        ? WritableAccount<TAccountRentPayee>
        : TAccountRentPayee,
      ...TRemainingAccounts,
    ]
  >;

export type CloseStealthAccountInstructionData = { discriminator: number };

export type CloseStealthAccountInstructionDataArgs = {};

export function getCloseStealthAccountInstructionDataEncoder(): FixedSizeEncoder<CloseStealthAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_STEALTH_ACCOUNT_DISCRIMINATOR,
    }),
  );
}

export function getCloseStealthAccountInstructionDataDecoder(): FixedSizeDecoder<CloseStealthAccountInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getCloseStealthAccountInstructionDataCodec(): FixedSizeCodec<
  CloseStealthAccountInstructionDataArgs,
  CloseStealthAccountInstructionData
> {
  return combineCodec(
    getCloseStealthAccountInstructionDataEncoder(),
    getCloseStealthAccountInstructionDataDecoder(),
  );
}

export type CloseStealthAccountInput<
  TAccountBurner extends string = string,
  TAccountStealthAccount extends string = string,
  TAccountRentPayee extends string = string,
> = {
  /** Burner keypair that owns the stealth account, proves ownership */
  burner: TransactionSigner<TAccountBurner>;
  /** Spent stealth PDA to close (deposited_amount must be zero) */
  stealthAccount: Address<TAccountStealthAccount>;
  /** Receives the reclaimed rent, normally the relayer */
  rentPayee: Address<TAccountRentPayee>;
};

export function getCloseStealthAccountInstruction<
  TAccountBurner extends string,
  TAccountStealthAccount extends string,
  TAccountRentPayee extends string,
  TProgramAddress extends Address = typeof SHREDR_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CloseStealthAccountInput<
    TAccountBurner,
    TAccountStealthAccount,
    TAccountRentPayee
  >,
  config?: { programAddress?: TProgramAddress },
): CloseStealthAccountInstruction<
  TProgramAddress,
  TAccountBurner,
  TAccountStealthAccount,
  TAccountRentPayee
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SHREDR_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    burner: { value: input.burner ?? null, isWritable: false },
    stealthAccount: { value: input.stealthAccount ?? null, isWritable: true },
    rentPayee: { value: input.rentPayee ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("burner", accounts.burner),
      getAccountMeta("stealthAccount", accounts.stealthAccount),
      getAccountMeta("rentPayee", accounts.rentPayee),
    ],
    data: getCloseStealthAccountInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseStealthAccountInstruction<
    TProgramAddress,
    TAccountBurner,
    TAccountStealthAccount,
    TAccountRentPayee
  >);
}

export type ParsedCloseStealthAccountInstruction<
  TProgram extends string = typeof SHREDR_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Burner keypair that owns the stealth account, proves ownership */
    burner: TAccountMetas[0];
    /** Spent stealth PDA to close (deposited_amount must be zero) */
    stealthAccount: TAccountMetas[1];
    /** Receives the reclaimed rent, normally the relayer */
    rentPayee: TAccountMetas[2];
  };
  data: CloseStealthAccountInstructionData;
};

export function parseCloseStealthAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseStealthAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      burner: getNextAccount(),
      stealthAccount: getNextAccount(),
      rentPayee: getNextAccount(),
    },
    data: getCloseStealthAccountInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
