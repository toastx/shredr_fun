/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const STEALTH_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [83, 72, 82, 69, 68, 82, 83, 65],
);

export function getStealthAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    STEALTH_ACCOUNT_DISCRIMINATOR,
  );
}

/** State stored inside every stealth PDA. */
export type StealthAccount = {
  /** Fixed 8-byte discriminator, ASCII `SHREDRSA`. */
  discriminator: ReadonlyUint8Array;
  /** The burner pubkey that owns this stealth account. */
  owner: Address;
  /** Reserved, always zero. The PDA is derived from the burner alone. */
  salt: ReadonlyUint8Array;
  /** Lamports deposited, excluding the rent-exempt minimum. */
  depositedAmount: bigint;
  /** Unix timestamp of the initial deposit. */
  depositTimestamp: bigint;
  /** Whether the account is currently delegated to a MagicBlock validator. */
  delegated: boolean;
  /** PDA bump seed. */
  bump: number;
  /**
   * Which leg of a cycle this PDA is: 0 unset, 1 deposit, 2 exit.
   * Carved out of the trailing padding, so the account is still 96 bytes
   * and accounts written before this field read back as unset.
   * A recovery hint only — the program never authorizes on it.
   */
  role: number;
  /** `#[repr(C)]` trailing padding. */
  padding: ReadonlyUint8Array;
};

export type StealthAccountArgs = {
  /** The burner pubkey that owns this stealth account. */
  owner: Address;
  /** Reserved, always zero. The PDA is derived from the burner alone. */
  salt: ReadonlyUint8Array;
  /** Lamports deposited, excluding the rent-exempt minimum. */
  depositedAmount: number | bigint;
  /** Unix timestamp of the initial deposit. */
  depositTimestamp: number | bigint;
  /** Whether the account is currently delegated to a MagicBlock validator. */
  delegated: boolean;
  /** PDA bump seed. */
  bump: number;
  /**
   * Which leg of a cycle this PDA is: 0 unset, 1 deposit, 2 exit.
   * Carved out of the trailing padding, so the account is still 96 bytes
   * and accounts written before this field read back as unset.
   * A recovery hint only — the program never authorizes on it.
   */
  role: number;
  /** `#[repr(C)]` trailing padding. */
  padding: ReadonlyUint8Array;
};

/** Gets the encoder for {@link StealthAccountArgs} account data. */
export function getStealthAccountEncoder(): FixedSizeEncoder<StealthAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["salt", fixEncoderSize(getBytesEncoder(), 32)],
      ["depositedAmount", getU64Encoder()],
      ["depositTimestamp", getI64Encoder()],
      ["delegated", getBooleanEncoder()],
      ["bump", getU8Encoder()],
      ["role", getU8Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 5)],
    ]),
    (value) => ({ ...value, discriminator: STEALTH_ACCOUNT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link StealthAccount} account data. */
export function getStealthAccountDecoder(): FixedSizeDecoder<StealthAccount> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["salt", fixDecoderSize(getBytesDecoder(), 32)],
    ["depositedAmount", getU64Decoder()],
    ["depositTimestamp", getI64Decoder()],
    ["delegated", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["role", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 5)],
  ]);
}

/** Gets the codec for {@link StealthAccount} account data. */
export function getStealthAccountCodec(): FixedSizeCodec<
  StealthAccountArgs,
  StealthAccount
> {
  return combineCodec(getStealthAccountEncoder(), getStealthAccountDecoder());
}

export function decodeStealthAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<StealthAccount, TAddress>;
export function decodeStealthAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<StealthAccount, TAddress>;
export function decodeStealthAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<StealthAccount, TAddress> | MaybeAccount<StealthAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getStealthAccountDecoder(),
  );
}

export async function fetchStealthAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<StealthAccount, TAddress>> {
  const maybeAccount = await fetchMaybeStealthAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeStealthAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<StealthAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeStealthAccount(maybeAccount);
}

export async function fetchAllStealthAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<StealthAccount>[]> {
  const maybeAccounts = await fetchAllMaybeStealthAccount(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeStealthAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<StealthAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeStealthAccount(maybeAccount),
  );
}
